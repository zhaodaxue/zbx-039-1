version: '3.8'

services:
  coffee-roast-tracker:
    build: .
    container_name: coffee-roast-tracker
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - PORT=3001
    volumes:
      - ./api/data:/app/api/data
    restart: unless-stopped
